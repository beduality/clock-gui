[project]
name = "clock-time"
version = "0.4.0"
description = "Localized in-game time displays for Minecraft"
requires-python = ">=3.13"
dependencies = [
    "cyclopts>=4.20.0",
    "mkdocs-material>=9.7.6",
]
